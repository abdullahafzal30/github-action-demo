name: My First GitHub Action

on:
  push:
    branches:
      - main

jobs:
  demo-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello Message
        run: echo "🎉 Mubarak ho! GitHub Action successfully chal rahi hai"

